import { addVectorsCopy } from "../matrix.js";

export class StarSector {
    constructor(pos, size, stars) {
        this.pos = pos;
        this.size = size;

        this.loadStars(stars);
    }

    loadStars(stars) {
        
    }
    
}